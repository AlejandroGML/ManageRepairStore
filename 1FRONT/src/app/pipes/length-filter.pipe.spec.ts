import { LengthFilterPipe } from './length-filter.pipe';

describe('LengthFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
